["sakura provisioning"]
- type = ""
- host_name = ""
- internal_subtypes = {}

if(internal_subtypes.contains("ssh_provisioning") == true)
{
    print("debug-print")
    - type = type
    - host_name = host_name
    - internal_subtypes = internal_subtypes
}
else
{
    print("fail")
    - fail = "fail"
}
